version: '3'

services:
    php:
        build:
            context: ./php8
        container_name: eventsauce_upcasting_php
        volumes:
            - ../../:/var/library/src:rw
        networks:
            - default
networks:
    default:
